{
  "name": "chatplug-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node backend/server.js",
    "dev": "concurrently \"npm run dev --prefix frontend\" \"PORT=5000 npm run dev --prefix backend\"",
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix backend",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "puppeteer": "^24.42.0"
  },
  "dependencies": {
    "@google/genai": "^1.51.0",
    "@xenova/transformers": "^2.17.2"
  }
}
